@reboot /usr/local/bin/watchtower --run-once open-webui  # Immediate run at startup (handled by cron)
0 0 * * * /usr/local/bin/watchtower open-webui --interval INTERVAL_PLACEHOLDER  # Recurring job (placeholder)